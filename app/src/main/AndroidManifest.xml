<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sunith.pacy">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logobaby"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logobaby"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".about"></activity>
        <activity android:name=".startpage"
            android:theme="@style/AppTheme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
            </activity>
        <activity android:name=".advance"></activity>
        <activity android:name=".begg"></activity>
        <activity android:name=".med"></activity>
        <activity android:name=".ques"></activity>
        <activity android:name=".site"></activity>
    </application>

</manifest>